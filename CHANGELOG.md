# 更新日志

版本变更记录，是 GitHub Release 说明的来源。

## 未发布

## v0.4.4 - 2026-09-26

### Added
- 新增副歌词字体设置：原「第二行」改称「副歌词」，字体、字号、字重和斜体可与主歌词分开设置。
- 新增本曲偏移：开启全局偏移后，每首歌仍可单独微调，与全局偏移叠加生效；「全局设置」页可直接修改当前歌曲的偏移。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.4.3...v0.4.4

## v0.4.3 - 2026-09-26

### Added
- 新增逐字「粒子」：唱到每个词时从字上飘起几颗大小不一的小光点，约 2 秒后熄灭；暗色外观默认开启、亮色外观默认关闭，可在外观页「逐字」分组设置，颜色默认跟随「正在唱」，也可单独设置。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.4.2...v0.4.3

## v0.4.2 - 2026-09-24

### Breaking Changes
- 移除构建选项 `ENABLE_PROVIDER_NETEASE`、`ENABLE_PROVIDER_AMLL`、`ENABLE_PROVIDER_QQ`，所有歌词源固定编译，`zlib` 成为必需的构建依赖；运行时仍可在「歌词源」页启停和排序。**迁移**：从构建命令中删除这些选项（设为 `OFF` 会导致配置失败），已有构建目录可用 `cmake -S . -B build -U 'ENABLE_PROVIDER_*'` 清理。
- 更改桌面形态的行高下限为 125%，面板形态仍可设到 100%。**迁移**：无需操作，原先低于 125% 的设置按 125% 显示。

### Added
- 新增亮色、暗色两套外观：外观页新增「主题模式」（跟随系统／始终亮色／始终暗色），歌词与曲目信息的颜色可按亮暗分别设置，跟随系统时随 Plasma 外观样式自动切换；升级前改过颜色的部件，两套都沿用原设置。
- 新增字体设置：歌词与曲目信息可分别选择字体，支持按中英文名搜索；新安装的字体需重启 `plasmashell` 后才会出现在列表中。
- 新增「模拟逐字」（默认关闭）：歌词源不提供逐字歌词时，按行时长模拟逐字高亮。
- 新增设置变更日志：修改设置时记录一行日志，便于排查问题；服务运行时可用 `journalctl --user -u plasma-lyricsd` 查看，代理地址中的账号密码不会记录。
- 新增 AUR 二进制包 `plasma-lyrics-bin`，无需本地编译，与 `plasma-lyrics`、`plasma-lyrics-git` 三选一安装。

### Changed
- 更改「字重」选项为当前字体实际拥有的字重，不再合成粗体；原先选择的字重该字体没有时，按最接近的一档显示。
- 更改「无歌词时文案」的默认值为「纯音乐，请欣赏」，已自定义的文案不受影响。
- 更改逐字歌词的「光晕」：光晕会略微超出本行范围，行高越小越明显。
- 更改外观页的「逐字」为「逐字歌词」，并在说明中注明需要歌词源支持（推荐 QQ音乐 与 AMLL）。
- 更改依赖：新增 `fontconfig`，Debian 下构建需安装 `libfontconfig-dev`。

### Fixed
- 修复 `plasma-lyrics` AUR 包缺少 `zlib` 运行依赖的问题。
- 修复歌词中 y、g 等字母的下半部分被裁掉的问题。
- 修复在右键菜单切换歌词源时，已有缓存歌词仍重新联网搜索的问题。
- 修复 QQ音乐 逐字歌词含英文双引号时后半段歌词消失、部分曲目完全取不到 QQ音乐 歌词的问题；升级后已缓存的 QQ音乐 逐字歌词会在下次播放时重新获取，个别曲目可能需要重新调整单曲偏移。
- 修复 QQ音乐 逐字歌词含 `(1,2)` 这类文字时高亮错位、文字缺失的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.4.1...v0.4.2

## v0.4.1 - 2026-09-13

### Changed
- 更改逐字歌词的抬升动画：当前词起唱时迅速弹起，唱完后回落并轻微回弹，节奏不再随词长变化；开启抬升时行高略有增加。
- 更改「光晕」：光晕随字回落后再消失，快唱段可同时出现多个，绘制开销略有增加。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.4.0...v0.4.1

## v0.4.0 - 2026-09-13

概要：新增 QQ音乐歌词源与逐字歌词，第二行可显示翻译或罗马音；部分翻唱、伴奏类曲目升级后可能不再出歌词，见 Breaking Changes。

### Breaking Changes
- 更改带译名括号标题的匹配规则：还需艺人相似才会采用，部分翻唱、伴奏类曲目升级后不再出歌词。**迁移**：在音频文件旁放同名 `.lrc`（最可靠），或修正播放器中的艺人信息，或在「歌词源」页换一个源；可用 `plasma-lyricsd --explain` 查看原因。

### Added
- 新增 QQ音乐歌词源，提供逐字歌词与罗马音；默认排在最后，可在「歌词源」页调整顺序，或用右键菜单「此歌曲首选 QQ音乐」为单曲指定。
- 新增逐字歌词显示（默认开启）：当前词高亮并轻微抬升，颜色与效果可在外观页调整；需要歌词源支持（AMLL、QQ音乐）。
- 新增「光晕」效果（默认关闭），为正在唱的词添加光晕。
- 新增「第二行」设置，可显示翻译、罗马音或不显示；罗马音目前仅 QQ音乐 提供。
- 新增「行高」设置，范围 100% 到 200%，默认 125%。

### Changed
- 更改歌词源默认顺序为本地 → 网易云 → AMLL → QQ音乐，升级后已有曲目的歌词来源不变。

### Fixed
- 修复切歌后歌词偶尔停留在上一首的问题。
- 修复播放器把艺人名写进标题时匹配不到歌词的问题。
- 修复候选标题带译名括号时匹配不到歌词的问题。
- 修复 v0.3.2 起部分原本能匹配的曲目不再出歌词的问题。
- 修复启用 AMLL 歌词源时匹配偏慢的问题。
- 修复默认屏蔽 KDE Connect 的设置失效、手机上的播放被当作音乐的问题。
- 修复在「歌词服务」页取消勾选全部平台后，重启服务又恢复为全部启用的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.3.2...v0.4.0

## v0.3.2 - 2026-09-11

### Added
- 新增 `plasma-lyricsd --explain` 的 `--length-ms` 选项，可指定曲目时长。

### Changed
- 更改服务日志的显示：在 journal 中按级别着色，并可用 `journalctl -p` 按级别筛选；在终端运行时彩色输出。
- 更改日志中的分类名，省略 `plasmalyrics.` 前缀；journal 记录不再带 `QT_CATEGORY` 字段。

### Fixed
- 修复标题带中文译名括号（如 `青さは止んだ (青春已逝)`）时匹配不到歌词的问题；AMLL 歌词源暂不支持，本地歌词需在 `.lrc` 中标注 `[length:mm:ss]`。
- 修复歌词源的第一个搜索结果不合格时，整首歌都匹配不到歌词的问题。
- 修复启用 AMLL 歌词源时切歌响应慢的问题。
- 修复 `plasma-lyricsd --explain` 的诊断结果与实际匹配不一致的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.3.1...v0.3.2

## v0.3.1 - 2026-09-11

### Added
- 新增本地歌词的双语 LRC 支持：时间戳相同的相邻两行分别作为原文和译文显示；已缓存的歌词需右键「重新搜索歌词」后生效。
- 新增网络代理设置：「歌词服务」页可选直连、系统代理或自定义 `socks5`/`http` 地址，作用于网易云与 AMLL。

### Changed
- 更改设置对话框只显示当前部件形态的外观页：桌面部件不再显示「面板外观」，面板部件不再显示「桌面外观」。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.3.0...v0.3.1

## v0.3.0 - 2026-09-11

概要：新增本地与 AMLL TTML DB 歌词源及多源自动回退，可按单曲指定来源；新增调试日志开关。

### Added
- 新增 AMLL TTML DB 歌词源，提供翻译。
- 新增本地歌词源：优先读取音频文件旁的 `.lrc`，也可从指定目录按文件名匹配。
- 新增多歌词源自动回退，默认顺序为本地 → 网易云 → AMLL，可在「歌词服务」页排序、启停和调整各源设置。
- 新增右键菜单的歌词源操作：为当前歌曲指定首选来源、恢复自动或立即重新搜索，对所有部件同时生效。
- 新增「记录调试信息」开关（`logging/debug`），开启后服务日志记录更详细的信息，重启服务后生效。
- 新增按分类的服务日志，可在 `kdebugsettings` 中按分类开关。

### Changed
- 更改歌词缓存为按歌词源分别保存，升级后原有歌词与偏移保留。
- 更改 AMLL 歌词源的匹配方式：区分 Live、Remix、Cover、重制版等版本，避免用错版本的歌词。
- 更改服务日志格式：带时间戳、级别与分类，并记录每次歌词搜索的过程与结果。
- 更改「文本」页的默认文案开关：原先四个合并为一个，升级时自动迁移。
- 更改新建部件的默认外观：桌面歌词默认加粗，面板歌词默认淡入淡出切换且不显示翻译；已有部件不受影响。

### Fixed
- 修复播放期间桌面与面板周期性卡顿数秒的问题。
- 修复歌词目录较大时，播放未缓存的歌曲会造成卡顿的问题。
- 修复歌词加载后进度偏前、且偏差随播放时间增大的问题。
- 修复切换或关闭播放器时守护进程崩溃、歌词短暂中断的问题。
- 修复「歌词服务」页的改动按「应用」或「确定」后不生效的问题。
- 修复停用歌词源并重启服务后，该来源从设置页消失、无法重新启用的问题。
- 修复没有正在播放的歌曲时无法从部件菜单调整全局偏移的问题。
- 修复调整全局偏移可能意外触发重新搜索歌词的问题。
- 修复音频文件旁的 `.lrc` 损坏时，歌词目录中的有效歌词也不被使用的问题。
- 修复部分播放器读取不到音频文件旁 `.lrc` 的问题。
- 修复新增或修复音频文件旁的 `.lrc` 后仍显示无歌词的问题。
- 修复强制重新搜索后可能一直停在「搜索中」的问题。
- 修复单曲循环进入新一轮时不重试首选歌词源的问题。
- 修复 AMLL 歌词源可能匹配到其他版本歌词的问题。
- 修复部分 AMLL 歌词的时间轴与翻译解析不完整的问题。
- 修复更改 AMLL 地址后新地址不生效的问题。
- 修复调整播放倍速后歌词进度不同步的问题。
- 修复翻译行中混入背景人声、罗马音等文字的问题。
- 修复从 Waylyrics 导入的歌词显示为「无歌词源」的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.3...v0.3.0

## v0.2.3 - 2026-09-08

### Added
- 新增「保存并重启服务」按钮，保存全局设置后立即生效，并显示重启是否成功。
- 新增「此曲无歌词」与「网络错误」两种提示，文案均可自定义；「暂无歌词」改为默认显示。
- 新增网络失败自动重试，最多尝试 3 次。
- 新增歌词搜索全过程的日志，便于排查问题。

### Changed
- 更改网易云搜索与歌词获取为异步进行，等待网络时不再影响播放状态的更新。

### Fixed
- 修复临时网络故障后同一首歌 7 天内不再获取歌词的问题，现在 5 分钟后即可重试。
- 修复 HTTP 或解析错误被误报为网络错误的问题。
- 修复歌词下载中途断开时不重试的问题。
- 修复升级前自定义的「暂无歌词」文案不生效的问题。
- 修复日志文件无法写入时没有任何提示的问题。
- 修复 `plasma-lyricsd --explain` 对非 Apple Music 曲目给出错误诊断的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.2...v0.2.3

## v0.2.2 - 2026-09-07

### Added
- 新增全局歌词偏移：开启后所有歌曲共用同一偏移值，关闭后恢复每首歌各自的偏移。
- 新增「歌词服务」页的平台开关，可分别启用网易云音乐与 Apple Music（含 Cider、Sidra）的歌词匹配。

### Fixed
- 修复网页版 Apple Music（`music.apple.com`）不显示歌词的问题。
- 修复播放器提供本地化译名（如「偶像」对应「アイドル」）时搜不到歌词的问题。
- 修复 Apple Music 提供罗马字标题（如「Gunjou」对应「群青」）时搜不到歌词的问题。
- 修复歌手名为「英文名 (中文名)」写法（如 `BTS (防弹少年团)`）时有时搜不到歌词的问题。
- 修复按本地化译名搜索时可能匹配到错误的翻唱或 Remix 版本的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.1...v0.2.2

## v0.2.1 - 2026-09-06

### Added
- 新增 Debian 13 的 `.deb` 安装包。
- 新增 Arch 安装包与源码压缩包，随 GitHub Release 发布并附 `SHA256SUMS` 校验文件。

### Changed
- 更改 waylyrics 导入工具的名称为 `plasma-lyrics-import-waylyrics`，安装到 `/usr/bin` 可直接运行；原路径 `/usr/libexec/plasma-lyrics/import-waylyrics` 不再提供。

### Fixed
- 修复 Debian 等发行版上 `systemctl --user` 找不到守护进程服务的问题。
- 修复启用 debug 选项构建 AUR 包时测试失败的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.0...v0.2.1

## v0.2.0 - 2026-09-05

概要：新增自动隐藏与面板宽度设置，外观设置按形态拆分。

### Added
- 新增自动隐藏：播放空闲超过可配置的缓冲时间后部件自动隐藏，曲目开始时恢复；桌面部件淡出，面板部件归还占用空间。默认关闭。
- 新增「面板外观 → 宽度」设置，面板部件宽度可配置，像素单位，默认 280。

### Changed
- 外观设置按桌面/面板形态拆分，两种部件的配置互不影响；各标签页标明设置仅对当前部件生效。
- 面板部件默认宽度由约 144 px 调整为 280 px；升级后相邻部件会被推动，可在「面板外观 → 宽度」改回，最低 80 px。

### Fixed
- 修复中途退出跑马灯后歌词停在负偏移、滚出部分被裁剪的问题。
- 修复开启自动隐藏后部分主题丢失壁纸模糊的问题，现在底板仅在淡出期间自绘，其余时间归还 shell 绘制。
- 修复两个外观标签页标题翻译失效、回退为英文的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.1.1...v0.2.0

## v0.1.1 - 2026-09-04

### Added
- 新增歌词上方的常驻标题与歌手行：样式与歌词独立配置，桌面默认开启、面板默认关闭；在搜索中、未找到等空白状态下保持显示，可区分部件故障与歌曲无词。

### Changed
- 桌面部件的歌词区高度减少一行，可手动调整部件大小恢复。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.1.0...v0.1.1

## v0.1.0 - 2026-09-04

首个版本。

### Added
- 新增桌面与面板两种部件形态，外观配置各自独立。
- 新增底板样式（主题 / 纯色 / 无）、文字描边、字号、字重与颜色设置。
- 新增三种溢出策略（`fit` / `wrap` / `marquee`）与三种切行动画（无 / 淡入淡出 / 滑动）。
- 新增歌词翻译行显示。
- 新增右键菜单的单首歌曲时序偏移调整，步进 0.5 s，同曲所有部件共享。
- 新增手工 `.lrc` 覆盖，放置于 `~/.local/share/plasma-lyrics/overrides/<provider>:<track-id>.lrc`。
- 新增 SQLite 歌词缓存，含未找到的负缓存。
- 新增空闲与未找到提示文本自定义，及独立的「搜索中」状态。
- 新增 zh_CN 界面翻译。
- 新增 `plasma-lyricsd --explain "<歌名>" "<歌手>"` 离线匹配诊断。
- 新增 waylyrics JSON 缓存一次性导入工具。
