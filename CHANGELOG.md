# 更新日志

版本变更记录，是 GitHub Release 说明的来源。

## 未发布

### Breaking Changes
- 移除按歌词源裁剪构建的 `ENABLE_PROVIDER_NETEASE`、`ENABLE_PROVIDER_AMLL`、`ENABLE_PROVIDER_QQ` 选项，全部歌词源固定编译，zlib 为固定构建依赖；运行时仍可在「歌词源」页启停和排序。**迁移**：删除构建命令中的旧选项，并用 `cmake -S . -B build -U 'ENABLE_PROVIDER_*'` 清理已有缓存；旧选项值为 `OFF` 时配置会报错。
- 桌面形态的行高不能再设到 125% 以下；面板形态不受影响，仍可设到 100%。存量配置里更小的值一律按 125% 渲染，设置页里也会显示为 125%，但只有实际调整过这项设置并保存后，配置文件里的值才会一并变成 125%。**迁移**：没有恢复到更紧行距的方法。

### Added
- 新增设置变更日志：「歌词服务」「全局设置」两页保存、桌面外观/面板外观/文本三页的每项设置、以及「保存并重启服务」都会记一行；部件这一份可用 `journalctl --user QT_CATEGORY=plasmalyrics.config` 查看，守护进程收到的相同一行计入 `journalctl --user -u plasma-lyricsd` 与可选日志文件（服务未运行时只有部件那一份），`network/proxyUrl` 记录时不带凭据。
- 新增 AUR 二进制包 `plasma-lyrics-bin`，安装时直接取用发布好的构建产物，不在本地编译；它与 `plasma-lyrics`、`plasma-lyrics-git` 互斥，三者装其一即可。运行期依赖与源码包完全相同。
- 新增「模拟逐字」：歌词源整篇都不提供词级时间轴时，可选按字符均分每行时长模拟逐字高亮，桌面与面板外观页各一个开关（默认关闭）。与真逐字共用同一套抬升、提亮、光晕效果，会像真逐字一样让对应曲目在播放时耗电和占用更高。
- 新增亮色、暗色两套外观：桌面外观、面板外观页顶部新增「主题模式」（跟随系统／始终亮色／始终暗色，默认跟随系统），歌词、逐字歌词与曲目信息颜色的设置分为「亮色」「暗色」两个标签页各存一套，曲目信息的其余设置、面板宽度与自动隐藏两套共用。跟随系统时，按当前 Plasma 外观样式（即部件底板与面板的样式）是亮色还是暗色换用对应的一套，样式切换后颜色渐变过渡；系统关闭动画时直接切换。标签页底部的「从暗色主题同步」「从亮色主题同步」按钮确认后把另一套设置整套复制过来，点击「应用」或「确定」后生效。升级前改过歌词、逐字歌词或曲目信息颜色中任一项的部件，两套都沿用原来的设置；其余部件与新添加的部件，亮色一套默认为深色文字，底板仍随 Plasma 外观样式，默认不提亮正在唱的词。
- 新增字体设置：桌面外观、面板外观页各新增歌词用的「字体」下拉框（主行与第二行共用，默认「跟随系统字体」）和曲目信息用的「字体」下拉框（默认「与歌词相同」）。下拉框顶部有搜索框，按名字模糊匹配，中文字体的中英文名都能搜到；列表只含可缩放、且支持拉丁或中日韩文字的已安装字体。所选字体被卸载后改用系统字体显示，设置页里标为「（未安装）」，重新安装并重启 plasmashell 后自动恢复。部件运行期间新安装的字体，要重启 plasmashell 后才会出现在列表中。

### Changed
- 更改逐字歌词的「光晕」：开启时，高亮词的模糊辉光会略微越过本行范围、贴近下一行文字；行高设得越小越明显。
- 更改「无歌词时文案」的默认文案，源站确认无词的曲目改为显示「纯音乐，请欣赏」；自定义过该文案的配置不受影响。
- 更改设置页文案：外观页的「逐字」改名为「逐字歌词」，其说明改为点明需要歌词源支持（推荐首选 QQ音乐 与 AMLL 歌词源），「模拟逐字」与「光晕」的说明同步重写，两条补充说明改用与「记录调试信息」相同的浅色小字；三个前端页顶部的提示改为「这些设置只影响当前部件，其余部件不受影响」。各项行为不变。
- 更改「字重」下拉框：选项改为当前字体实际拥有的字重，不再固定 6 档；字体只有一种字重时字重框不可选，换字体时字重自动改为新字体里最近的一档。配置里的字重是当前字体没有的一档时，按最近的一档显示和渲染，不再合成粗体，例如默认字体下选过「半粗体」的配置现在显示为「粗体」。中文界面里 900 一档改名为「极粗体」，「特粗体」改指新增的 800 一档。
- 更改构建依赖：新增 fontconfig 开发文件（Debian 为 `libfontconfig-dev`），AUR 包的运行依赖新增 `fontconfig`。

### Fixed
- 修复 `plasma-lyrics` 源码包的运行依赖列表遗漏 `zlib` 的问题，安装时明确要求该运行库。
- 修复歌词行下伸部（如 y、g 的尾巴）在 100% 与 125% 行高下均被裁掉的问题。
- 修复在右键菜单选择已缓存该曲歌词的源（如「此歌曲首选 QQ音乐」或「歌词源：自动」）时仍重新联网搜索、源站失败时改为显示「当前歌词：网易云（临时回退）」一类回退状态的问题。现在直接显示缓存歌词；「重新搜索歌词」仍重新联网搜索。
- 修复 QQ音乐 部分逐字歌词播放到一半后消失的问题：歌词里带英文双引号（如 `"今日"`）时，引号之后的歌词全部丢失；提供逐字歌词、且歌名、歌手或专辑名带英文双引号的曲目此前完全取不到 QQ音乐 歌词。
- 修复 QQ音乐 逐字歌词中含形如 `(1,2)` 的文字时，该处逐字高亮错位、文字缺失的问题。

## v0.4.1 - 2026-09-13

### Changed
- 更改逐字歌词的抬升动画：当前词起唱后约 150 ms 弹到位并停住，词结束后约 300 ms 回落并轻微回弹，起落快慢不再随词长变化；提亮同步跟随，面板形态的提亮同样保持到词结束后再暗回。开启抬升时行高比之前多 1 到 4 像素，字号越大越明显。
- 更改「光晕」：光晕随字回落后再消失，快唱段会同时出现多个，绘制开销相应增加。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.4.0...v0.4.1

## v0.4.0 - 2026-09-13

概要：新增 QQ音乐歌词源与逐字歌词显示，第二行可选翻译或罗马音；带译名括号的候选新增艺人相似度要求，部分曲目升级后不再出歌词，迁移办法见下。

### Breaking Changes
- 候选标题带尾部译名括号、且需剥掉括号才能匹配查询标题时，现在要求两侧艺人相似，任一侧缺艺人信息即视为不相似。受影响的主要是翻唱、伴奏带与 `Originally Performed by X`、`(BR)` 一类候选，部分曲目升级后不再出歌词。**迁移**：①在音频文件旁放同名 `.lrc`，不经该判定，最可靠；②修正播放源的艺人元数据；③在「歌词源」页换一个源，不保证有效。用 `plasma-lyricsd --explain` 检查，受影响候选显示 `rejected=candidate-gloss-artist-threshold`。

### Added
- 新增逐字歌词显示（默认开启，可在桌面与面板外观页分别关闭）：当前词整词换色并平滑抬升、提亮，未唱／正在唱／已唱三档颜色与抬升、提亮幅度可配置；过长歌词选「跑马灯」时改为跟随当前词滚动。词级时间轴由 AMLL 与 QQ音乐提供，其余来源仍按整行显示；面板外观页不提供抬升。
- 长行在「缩小字号」「截断」下缩到最小字号仍放不下时自动退回整行显示，避免正在唱的词被裁出可视区。
- 新增「光晕」开关（默认关闭）：为当前词叠加模糊光晕，绘制开销高于仅提亮。
- 新增「第二行」设置：可选翻译、罗马音或不显示，桌面与面板各一套，颜色可单独设置。罗马音由 QQ音乐提供，其余来源选中后第二行为空。
- 新增「行高」设置：100% 到 200%，默认 125%。
- 新增 `plasma-lyricsd --explain` 候选行的 `plainTitle=`／`durationGate=` 字段：标明候选不剥离本身能打到的分数，以及是否因此需要时长限制。
- 新增 QQ音乐歌词源：第四个独立源，排在既有串行回退链末尾，有自己的曲目 id 与单曲偏移，提供逐字歌词与词级罗马音（当前唯一提供罗马音的源）。编译开关 `ENABLE_PROVIDER_QQ` 关闭时不引入 zlib 依赖。
- 新增罗马音缓存：逐词与整行两种形式随歌词一并缓存，整行罗马音可在「第二行」显示，词级罗马音本次仅存储。
- 新增后端设置 `providers/qq/searchBaseUrl`、`providers/qq/lyricBaseUrl`、`providers/qq/timeoutMs`，默认分别为 `https://u.y.qq.com`、`https://c.y.qq.com`、4000 毫秒。
- 新增对 QQ音乐曲名行与制作人员行的过滤，交由既有的「过滤开头制作人员」开关处理。
- 新增对 QQ音乐纯音乐曲目的识别：该源返回的提示文本按「没有歌词」处理，不再当作歌词显示。
- QQ音乐取不到逐字歌词时回退到该源的整行歌词，整首歌仍可用。

### Changed
- 歌词源默认顺序改为 `local` → `netease` → `amll` → `qq`：QQ音乐排在末尾，升级不改变任何一首歌的现有来源；要优先使用需在「歌词源」页启用并拖到前面，单曲可用右键菜单「此歌曲首选 QQ音乐」。
- `filter/platforms` 改用与 `players/blacklist` 相同的「未设置」与「显式设为空」区分：此前两个平台都取消勾选会在配置里留下 `@Invalid()`，现在写入显式标记，守护进程重读配置后不再恢复为两个平台都启用。**升级期提示**：旧插件进程尚未重启时保存设置会覆盖该空状态。

### Fixed
- 修复切歌后歌词仍停留上一首：新歌当前行序号与上一首相同时，歌词与翻译不再刷新。
- 修复播放源把艺人拼进标题字段时匹配不到歌词：剥离该词后的标题也参与打分，需两侧时长相差不超过 2 秒。
- 修复候选不剥离本身已达标、却因剥离命中分数更高被时长判定误拒：查询侧或候选侧任一缺时长时不再限时，两侧时长都已知且相差超过 2 秒仍拒绝。此为 v0.3.2 引入的回归。
- 修复启用 AMLL 歌词源时匹配偏慢：标题归一化改为静态正则，约三千条本地索引上一次匹配从约 230 毫秒降至约 67 毫秒。
- 修复内置 kdeconnect 默认黑名单失效、手机转发的播放被当作音乐：`players/blacklist` 与 `filter/musicUrlPrefixes` 现在区分「未设置」与「显式设为空」，被清空的默认值在 `plasma-lyricsd` 重启后恢复；打开设置对话框会顺带清理残留的 `@Invalid()`。
- 修复 `filter/musicUrlPrefixes` 写成裸键时含义相反：此前按「放行任何带 URL 的播放器」处理，现在按「未配置自定义前缀」处理。
- 修复候选标题带译名括号时匹配不到歌词：括号剥离后也参与打分，采用条件见上方 Breaking Changes。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.3.2...v0.4.0

## v0.3.2 - 2026-09-11

### Added
- 新增 `plasma-lyricsd --explain` 的 `--length-ms` 选项，用于提供曲目时长；诊断输出新增查询时长一行，未提供时长且候选因此被拒时会给出提示。

### Changed
- 更改服务日志的输出形态：在 systemd journal 下不再重复打印时间戳与级别，改为按消息级别设置 syslog 优先级，`journalctl` 因此原生按级别着色，`journalctl -p 4` 一类的优先级筛选也开始生效；在终端直接运行时按级别输出 ANSI 彩色并保留时间戳与级别；重定向到文件或管道时保持原有的纯文本格式。
- 更改日志中的分类显示名：三种输出形态与日志文件都省略 `plasmalyrics.` 前缀，`plasmalyrics.resolver` 显示为 `resolver`。`QT_LOGGING_RULES` 与 `kdebugsettings` 仍须使用完整分类名。
- 更改默认配置（未开启 `logging/fileEnabled`）下的日志写入方式：此前由 Qt 直接写入 journald 并附带结构化字段 `QT_CATEGORY`，现在统一由服务自身格式化，该字段不再出现，分类名改为出现在消息文本中。
- 更改「另一个实例已在运行」的日志级别为 warning，不再出现在 `journalctl -p 3` 一类只看错误的结果中。退出码不变。

### Fixed
- 修复标题带中文译名括号（如 `青さは止んだ (青春已逝)`）时匹配不到歌词：括号内不含版本标记词时，剥离括号后的标题也参与打分；靠剥离匹配上的候选需要两侧时长相差 2 秒以内佐证，任一侧时长未知时不予采用。AMLL 歌词源的索引不提供时长，该源上的此类标题仍匹配不到；本地歌词目录可通过在 `.lrc` 中补 `[length:mm:ss]` 标签启用。
- 修复首位候选被规则否决时整首歌都匹配不到歌词：改为跳过被否决的候选继续检查下一个，候选分数不足仍按原样停止。
- 修复启用 AMLL 歌词源时每次切歌主线程阻塞较久：约三千条本地索引的一次匹配耗时从约 1.4 秒降至约 0.18 秒。
- 修复 `--explain` 的诊断结论与服务实际行为相反：该命令此前无法提供时长，凡需剥离译名括号才能匹配的候选一律显示为因时长未知被拒。
- 修复两个网络歌词源同时编译关闭时 `--explain` 的可用源诊断期望值错误，该构建组合此前未被 CI 覆盖。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.3.1...v0.3.2

## v0.3.1 - 2026-09-11

### Added
- 新增本地歌词与覆盖文件的双语 LRC 支持：相邻两行时间戳相同时，第一行为原文、第二行为译文，同一时间戳只取前两行，第三行起忽略；任一行像制作人员信息时两行原样保留；已缓存的歌词需右键「重新搜索歌词」后生效。
- 新增「歌词服务」设置页的网络代理：可选直连、系统代理或自定义 `socks5`/`http` 地址，作用于网易云与 AMLL；地址非法时无法保存，已保存的非法地址会使这两个来源在服务重启后不可用。

### Changed
- 更改设置对话框侧栏分类：桌面部件不再显示「面板外观」，面板部件（水平或垂直）不再显示「桌面外观」，打开设置时也各自直接落在对应的外观页；「文本」「全局设置」「歌词服务」两种形态都显示。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.3.0...v0.3.1

## v0.3.0 - 2026-09-11

概要：新增本地与 AMLL TTML DB 歌词源及多源串行回退，可按单曲指定来源；服务日志改为分类输出并新增调试开关。

### Added
- 新增 AMLL TTML DB 歌词源：缓存并条件刷新元数据索引，本地匹配后按需下载 TTML，保存主歌词、翻译、逐词时间与来源信息。
- 新增可搜索的本地歌词源：优先读取音频同级 `.lrc`，再按文件名和 LRC ID 标签匹配配置目录中的歌词。
- 新增多歌词源串行回退，默认顺序为本地 → 网易云 → AMLL；可在「歌词服务」设置页拖拽排序、勾选启用来源并调整各源参数。
- 新增当前歌曲的歌词源右键操作：指定本地文件、网易云或 AMLL 为首选、恢复自动，以及绕过旧命中和负缓存立即重搜；获取期间会显示目标源并暂时禁用重复操作。
- 新增会话 D-Bus 控制接口与快照来源字段，多个部件实例共享单曲偏好、实际来源和临时回退状态。
- 新增「歌词服务」设置页「诊断」下的「记录调试信息」开关（`logging/debug`），为服务日志打开更详细的调试信息（请求地址、匹配打分、播放器事件等），重启服务后生效。
- 新增按分类的服务日志（`plasmalyrics.daemon`/`.resolver`/`.mpris`/`.provider.netease`/`.provider.amll`/`.provider.local`），在 `kdebugsettings` 中可按分类开关；也可用 `QT_LOGGING_RULES` 环境变量单独控制某一分类，优先于设置页开关。

### Changed
- SQLite 缓存升级为按 provider 保存结果映射与失败冷却，旧有效歌词、偏移和映射继续保留，旧全局失败记录不会抑制新增歌词源。
- AMLL 使用保留 Live、Remix、Cover、重制版等后缀的版本分层匹配；冲突版本直接排除，普通候选始终优先于单边后缀候选。
- 更改服务日志格式：统一加上时间戳、级别与分类前缀，每次歌词解析以请求编号关联全部相关日志行，记录触发原因、各歌词源的搜索候选与最终结果。
- 更改默认日志详细程度：候选打分明细与缓存查找细节改为仅在开启「记录调试信息」后记录。
- 更改服务的退出流程：收到 `SIGTERM`/`SIGINT` 时优雅退出并记录停止原因。
- 更改「文本」设置页的空文案处理：原先四个「使用本地化默认文案」开关合并为一个，非空自定义文案始终优先，仅在字段留空时由该开关决定显示本地化默认文案还是保持空白；升级自动迁移，原先任一开关仍在使用默认文案即保持开启。
- 更改新建部件的默认外观：桌面歌词字重默认加粗，面板歌词切换动画默认为淡入淡出、默认不显示翻译；已有部件保留原有设置。

### Fixed
- 修复大歌词目录在每首未缓存歌曲上重复解析全部文件、阻塞播放位置轮询的问题；目录及子目录现在只在内容版本变化时重建索引。
- 修复损坏的音频同级 `.lrc` 会遮蔽歌词目录中有效匹配的问题；无有效时间行时会继续搜索目录。
- 修复全局偏移模式在没有当前歌曲时无法从部件菜单调整，以及刷新全局偏移可能意外触发完整歌词重搜的问题。
- 修复设置页拒绝停用最后一个歌词源后，勾选框不再跟随来源模型的问题。
- 修复停用歌词源并重启服务后，该来源从设置页消失、无法重新启用的问题；停用不会改变原有优先级位置。
- 修复普通 MPRIS 播放器只提供标准 `xesam:url` 时无法读取音频同级歌词，以及后来新增或修复同级歌词文件仍可能被旧失败冷却遮蔽的问题。
- 修复「歌词服务」页的改动在按 Apply 或 OK 后未生效的问题。
- 修复播放期间桌面与面板可能周期性卡顿数秒的问题。
- 修复歌词加载完成后进度向前偏移，且偏移量随已播放时间或取词耗时增长的问题。
- 修复切换或关闭播放器的瞬间守护进程崩溃、歌词短暂中断的问题。
- 修复 AMLL 官方 TTML 中无单位秒时间、官方关联翻译、跨 `span` 空白和 BCP-47 中文变体解析不完整的问题。
- 修复强制重搜在没有有效旧歌词时可能停留在 `searching` 或旧错误状态、没有发布最终结果的问题。
- 修复单曲循环进入新播放轮次时不重试首选歌词源，以及曲尾手动拖回开头会被误当作新轮次的问题；手动 seek 仍只重新对轴。
- 修复 AMLL 历史记录经后续桥接关联后未合并，以及冲突标题、艺人或 ISRC 可能被错误合并的问题。
- 修复更改 AMLL 索引或内容地址后复用旧条件请求头和失败冷却、从而抑制新镜像的问题。
- 修复未编译网易云 provider 时 `--explain` 不可用的问题；诊断现在支持实际 provider 链以及显式 `--provider`、`--platform`。
- 修复 AMLL 无版本别名可能掩盖 Live/Remix 冲突、不同年份重制版或不同 edition（含周年序号）可能被当作同版本，以及本地化标题兜底可能重新选中冲突版本的问题。
- 修复 AMLL 过期索引并行刷新时，旧歌词获取失败可能误使用新索引版本写入负缓存、抑制新候选的竞态。
- 修复 MPRIS 仅更新播放倍速时歌词时钟键点没有同步发布、常规位置轮询后倍速更新重复累计时间，以及 Apple 风格翻译中背景人声、罗马音和发音文本误混入主翻译的问题。
- 修复首选源与 provider 缓存重映射失败缺少日志、`--explain` 回调引用环泄漏，以及 Waylyrics 导入缓存被显示为“无歌词源”的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.3...v0.3.0

## v0.2.3 - 2026-09-08

### Added
- 新增「保存并重启服务」按钮，可在保存全局歌词服务设置后立即让改动生效，并明确显示重启成功或失败。
- 新增解析全过程日志，可查看曲目指纹、缓存映射与正文命中、缓存写入失败、provider 错误、负缓存、获取结果和最终显示状态。
- 新增「此曲无歌词」与「网络错误，暂时无法获取」独立状态和可自定义文案，「暂无歌词」也改为默认显示；三者均可设为空文案。
- 新增网络失败自动重试：最多尝试 3 次，默认超时依次为 4、6、8 秒，HTTP 错误和响应解析错误不会重试。

### Changed
- 更改网易云搜索与歌词获取为全链路异步，等待网络时守护进程仍能持续处理播放器状态。

### Fixed
- 修复日志文件路径不可写时没有错误提示的问题，现在会在 journal 中写明路径和失败原因。
- 修复非 Apple Music 曲目的匹配诊断误报已启用本地化标题兜底的问题，现在会分别标明实际决策与反事实结果。
- 修复升级前自定义的「暂无歌词」文案被新默认开关遮蔽的问题。
- 修复已收到 HTTP 2xx 响应头后正文传输中断时没有重试的问题。
- 修复临时网络故障会让同一首歌连续 7 天停止重试的问题，现在网络负缓存 5 分钟后失效，确定无候选仍缓存 7 天。
- 修复 HTTP 或歌词响应解析错误被误报为网络错误的问题，并避免搜索失败时记录误导性的 `selected: none`。
- 修复 daemon 退出时日志文件指针仍可能在静态析构阶段失效的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.2...v0.2.3

## v0.2.2 - 2026-09-07

### Added
- 新增全局歌词进度调整：设置页「全局设置」标签新增开关，开启后所有歌曲共用同一个偏移值，不再按歌曲区分；关闭时逐首歌各自的偏移保持不变，改动立即对所有桌面歌词部件生效，无需重启服务。
- 新增「歌词服务」设置页的平台勾选：可分别开关网易云音乐、Apple Music（含 Cider、Sidra）的歌词匹配，自定义 URL 前缀框保留。

### Fixed
- 修复网页版 Apple Music（`music.apple.com`）完全不显示歌词的问题。
- 修复播放源报送本地化译名（如「偶像」对应日文原名「アイドル」）时搜不到歌词的问题。
- 修复部分艺人（如网易云收录为「`BTS (防弹少年团)`」这类「英文名（中文名）」写法的歌手）的歌曲有时搜不到歌词的问题。
- 修复 Apple Music 报送罗马字标题（如「Gunjou」对应日文原名「群青」）时搜不到歌词的问题。
- 修复本地化译名可能连带匹配到错误翻唱/remix 版本的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.1...v0.2.2

## v0.2.1 - 2026-09-06

### Added
- 新增 Debian 13 的 `.deb` 安装包。
- 新增 Arch 的 `.pkg.tar.zst` 与源码 tarball，随 GitHub Release 一并发布，附 `SHA256SUMS` 校验文件。

### Changed
- waylyrics 导入工具更名为 `plasma-lyrics-import-waylyrics` 并改装到 `/usr/bin`，可直接调用；原路径 `/usr/libexec/plasma-lyrics/import-waylyrics` 不再提供。

### Fixed
- 修复在 Debian 等多架构发行版上守护进程服务文件装错目录、`systemctl --user` 找不到服务的问题。
- 修复在启用 debug 选项的 Arch 环境下构建 AUR 包时，测试因找不到 fixture 而失败的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.0...v0.2.1

## v0.2.0 - 2026-09-05

概要：新增自动隐藏与面板宽度设置，外观设置按形态拆分。

### Added
- 新增自动隐藏：播放空闲超过可配置的缓冲时间后部件自动隐藏，曲目开始时恢复；桌面部件淡出，面板部件归还占用空间。默认关闭。
- 新增「面板外观 → 宽度」设置，面板部件宽度可配置，像素单位，默认 280。

### Changed
- 外观设置按桌面/面板形态拆分，两种部件的配置互不影响；各标签页标明设置仅对当前部件生效。
- 面板部件默认宽度由约 144 px 调整为 280 px；升级后相邻部件会被推动，可在「面板外观 → 宽度」改回，最低 80 px。

### Fixed
- 修复中途退出跑马灯后歌词停在负偏移、滚出部分被裁剪的问题。
- 修复开启自动隐藏后部分主题丢失壁纸模糊的问题，现在底板仅在淡出期间自绘，其余时间归还 shell 绘制。
- 修复两个外观标签页标题翻译失效、回退为英文的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.1.1...v0.2.0

## v0.1.1 - 2026-09-04

### Added
- 新增歌词上方的常驻标题与歌手行：样式与歌词独立配置，桌面默认开启、面板默认关闭；在搜索中、未找到等空白状态下保持显示，可区分部件故障与歌曲无词。

### Changed
- 桌面部件的歌词区高度减少一行，可手动调整部件大小恢复。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.1.0...v0.1.1

## v0.1.0 - 2026-09-04

首个版本。

### Added
- 新增桌面与面板两种部件形态，外观配置各自独立。
- 新增底板样式（主题 / 纯色 / 无）、文字描边、字号、字重与颜色设置。
- 新增三种溢出策略（`fit` / `wrap` / `marquee`）与三种切行动画（无 / 淡入淡出 / 滑动）。
- 新增歌词翻译行显示。
- 新增右键菜单的单首歌曲时序偏移调整，步进 0.5 s，同曲所有部件共享。
- 新增手工 `.lrc` 覆盖，放置于 `~/.local/share/plasma-lyrics/overrides/<provider>:<track-id>.lrc`。
- 新增 SQLite 歌词缓存，含未找到的负缓存。
- 新增空闲与未找到提示文本自定义，及独立的「搜索中」状态。
- 新增 zh_CN 界面翻译。
- 新增 `plasma-lyricsd --explain "<歌名>" "<歌手>"` 离线匹配诊断。
- 新增 waylyrics JSON 缓存一次性导入工具。
