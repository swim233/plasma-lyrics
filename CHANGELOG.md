# 更新日志

版本变更记录，是 GitHub Release 说明的来源。

## 未发布

### Added
- 新增 AMLL TTML DB 歌词源：缓存并条件刷新元数据索引，本地匹配后按需下载 TTML，保存主歌词、翻译、逐词时间与来源信息。
- 新增多歌词源串行回退，默认顺序为网易云 → AMLL；可在「歌词服务」设置页调整全局顺序及 AMLL 网络参数。
- 新增当前歌曲的歌词源右键操作：指定网易云或 AMLL 为首选、恢复自动，以及绕过旧命中和负缓存立即重搜。
- 新增会话 D-Bus 控制接口与快照来源字段，多个部件实例共享单曲偏好、实际来源和临时回退状态。

### Changed
- SQLite 缓存升级为按 provider 保存结果映射与失败冷却，旧有效歌词、偏移和映射继续保留，旧全局失败记录不会抑制新增歌词源。
- AMLL 使用保留 Live、Remix、Cover、重制版等后缀的版本分层匹配；冲突版本直接排除，普通候选始终优先于单边后缀候选。

### Fixed
- 修复切换或关闭播放器的瞬间守护进程崩溃、歌词短暂中断的问题。
- 修复 AMLL 官方 TTML 中无单位秒时间、官方关联翻译、跨 `span` 空白和 BCP-47 中文变体解析不完整的问题。
- 修复强制重搜在没有有效旧歌词时可能停留在 `searching` 或旧错误状态、没有发布最终结果的问题。
- 修复单曲循环进入新播放轮次时不重试首选歌词源，以及曲尾手动拖回开头会被误当作新轮次的问题；手动 seek 仍只重新对轴。
- 修复 AMLL 历史记录经后续桥接关联后未合并，以及冲突标题、艺人或 ISRC 可能被错误合并的问题。
- 修复更改 AMLL 索引或内容地址后复用旧条件请求头和失败冷却、从而抑制新镜像的问题。
- 修复未编译网易云 provider 时 `--explain` 不可用的问题；诊断现在支持实际 provider 链以及显式 `--provider`、`--platform`。
- 修复 AMLL 无版本别名可能掩盖 Live/Remix 冲突、不同年份重制版或不同 edition（含周年序号）可能被当作同版本，以及本地化标题兜底可能重新选中冲突版本的问题。
- 修复 AMLL 过期索引并行刷新时，旧歌词获取失败可能误使用新索引版本写入负缓存、抑制新候选的竞态。
- 修复 MPRIS 仅更新播放倍速时歌词时钟键点没有同步发布、常规位置轮询后倍速更新重复累计时间，以及 Apple 风格翻译中背景人声、罗马音和发音文本误混入主翻译的问题。
- 修复首选源与 provider 缓存重映射失败缺少日志、`--explain` 回调引用环泄漏，以及 Waylyrics 导入缓存被显示为“无歌词源”的问题。

## v0.2.3 - 2026-09-08

### Added
- 新增「保存并重启服务」按钮，可在保存全局歌词服务设置后立即让改动生效，并明确显示重启成功或失败。
- 新增解析全过程日志，可查看曲目指纹、缓存映射与正文命中、缓存写入失败、provider 错误、负缓存、获取结果和最终显示状态。
- 新增「此曲无歌词」与「网络错误，暂时无法获取」独立状态和可自定义文案，「暂无歌词」也改为默认显示；三者均可设为空文案。
- 新增网络失败自动重试：最多尝试 3 次，默认超时依次为 4、6、8 秒，HTTP 错误和响应解析错误不会重试。

### Changed
- 更改网易云搜索与歌词获取为全链路异步，等待网络时守护进程仍能持续处理播放器状态。

### Fixed
- 修复日志文件路径不可写时没有错误提示的问题，现在会在 journal 中写明路径和失败原因。
- 修复非 Apple Music 曲目的匹配诊断误报已启用本地化标题兜底的问题，现在会分别标明实际决策与反事实结果。
- 修复升级前自定义的「暂无歌词」文案被新默认开关遮蔽的问题。
- 修复已收到 HTTP 2xx 响应头后正文传输中断时没有重试的问题。
- 修复临时网络故障会让同一首歌连续 7 天停止重试的问题，现在网络负缓存 5 分钟后失效，确定无候选仍缓存 7 天。
- 修复 HTTP 或歌词响应解析错误被误报为网络错误的问题，并避免搜索失败时记录误导性的 `selected: none`。
- 修复 daemon 退出时日志文件指针仍可能在静态析构阶段失效的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.2...v0.2.3

## v0.2.2 - 2026-09-07

### Added
- 新增全局歌词进度调整：设置页「全局设置」标签新增开关，开启后所有歌曲共用同一个偏移值，不再按歌曲区分；关闭时逐首歌各自的偏移保持不变，改动立即对所有桌面歌词部件生效，无需重启服务。
- 新增「歌词服务」设置页的平台勾选：可分别开关网易云音乐、Apple Music（含 Cider、Sidra）的歌词匹配，自定义 URL 前缀框保留。

### Fixed
- 修复网页版 Apple Music（`music.apple.com`）完全不显示歌词的问题。
- 修复播放源报送本地化译名（如「偶像」对应日文原名「アイドル」）时搜不到歌词的问题。
- 修复部分艺人（如网易云收录为「`BTS (防弹少年团)`」这类「英文名（中文名）」写法的歌手）的歌曲有时搜不到歌词的问题。
- 修复 Apple Music 报送罗马字标题（如「Gunjou」对应日文原名「群青」）时搜不到歌词的问题。
- 修复本地化译名可能连带匹配到错误翻唱/remix 版本的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.1...v0.2.2

## v0.2.1 - 2026-09-06

### Added
- 新增 Debian 13 的 `.deb` 安装包。
- 新增 Arch 的 `.pkg.tar.zst` 与源码 tarball，随 GitHub Release 一并发布，附 `SHA256SUMS` 校验文件。

### Changed
- waylyrics 导入工具更名为 `plasma-lyrics-import-waylyrics` 并改装到 `/usr/bin`，可直接调用；原路径 `/usr/libexec/plasma-lyrics/import-waylyrics` 不再提供。

### Fixed
- 修复在 Debian 等多架构发行版上守护进程服务文件装错目录、`systemctl --user` 找不到服务的问题。
- 修复在启用 debug 选项的 Arch 环境下构建 AUR 包时，测试因找不到 fixture 而失败的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.2.0...v0.2.1

## v0.2.0 - 2026-09-05

概要：新增自动隐藏与面板宽度设置，外观设置按形态拆分。

### Added
- 新增自动隐藏：播放空闲超过可配置的缓冲时间后部件自动隐藏，曲目开始时恢复；桌面部件淡出，面板部件归还占用空间。默认关闭。
- 新增「面板外观 → 宽度」设置，面板部件宽度可配置，像素单位，默认 280。

### Changed
- 外观设置按桌面/面板形态拆分，两种部件的配置互不影响；各标签页标明设置仅对当前部件生效。
- 面板部件默认宽度由约 144 px 调整为 280 px；升级后相邻部件会被推动，可在「面板外观 → 宽度」改回，最低 80 px。

### Fixed
- 修复中途退出跑马灯后歌词停在负偏移、滚出部分被裁剪的问题。
- 修复开启自动隐藏后部分主题丢失壁纸模糊的问题，现在底板仅在淡出期间自绘，其余时间归还 shell 绘制。
- 修复两个外观标签页标题翻译失效、回退为英文的问题。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.1.1...v0.2.0

## v0.1.1 - 2026-09-04

### Added
- 新增歌词上方的常驻标题与歌手行：样式与歌词独立配置，桌面默认开启、面板默认关闭；在搜索中、未找到等空白状态下保持显示，可区分部件故障与歌曲无词。

### Changed
- 桌面部件的歌词区高度减少一行，可手动调整部件大小恢复。

完整变更列表：https://github.com/swim233/plasma-lyrics/compare/v0.1.0...v0.1.1

## v0.1.0 - 2026-09-04

首个版本。

### Added
- 新增桌面与面板两种部件形态，外观配置各自独立。
- 新增底板样式（主题 / 纯色 / 无）、文字描边、字号、字重与颜色设置。
- 新增三种溢出策略（`fit` / `wrap` / `marquee`）与三种切行动画（无 / 淡入淡出 / 滑动）。
- 新增歌词翻译行显示。
- 新增右键菜单的单首歌曲时序偏移调整，步进 0.5 s，同曲所有部件共享。
- 新增手工 `.lrc` 覆盖，放置于 `~/.local/share/plasma-lyrics/overrides/<provider>:<track-id>.lrc`。
- 新增 SQLite 歌词缓存，含未找到的负缓存。
- 新增空闲与未找到提示文本自定义，及独立的「搜索中」状态。
- 新增 zh_CN 界面翻译。
- 新增 `plasma-lyricsd --explain "<歌名>" "<歌手>"` 离线匹配诊断。
- 新增 waylyrics JSON 缓存一次性导入工具。
