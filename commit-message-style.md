# 提交消息文本风格规范

适用：全部 commit message（feat / fix / docs / chore / merge / 发布提交）。
来源：2026-09 对本仓库 122 条历史消息的两轮规范化，以及此后新提交的复核；本文反例均为仓库内真实消息。

---

## 1. 三条原则

1. **读者是半年后的自己**：只写"改了什么、新行为是什么"，不写当时的心境与推理过程。
2. **可检索**：同一概念只有一种写法，便于 `git log --grep` 与回归定位。
3. **只写事实**：不评价（"更优雅"）、不展望（"后续再…"）、不含糊（"可能""大概"）。

---

## 2. 结构

```
type(scope): 中文主题

正文（0–2 句，可省略）
```

- 主题行**不带句号**；正文句末带「。」；主题与正文之间空一行。
- 不写 `Co-Authored-By` 等 trailer；署名信息由 git 的作者/提交者字段承载。
- 一次提交只装一个改动；多个不相关改动拆开，别用"并"硬拼。

---

## 3. 主题行

格式 `type(scope): 主题`——半角冒号 + 一个空格。

**type**：feat / fix / perf / docs / chore / merge。

**scope 用中文，且与仓库既有写法一致**。本仓库现有 scope：
`部件 面板 设置 外观 核心 匹配 守护进程 歌词源 本地源 本地歌词 数据获取 日志 解析 测试 构建 打包 配置 文档 诊断 MPRIS`。
新增 scope 前先 `git log --format='%s' | sed -n 's/^[a-z]*[(]\([^)]*\)[)].*/\1/p' | sort | uniq -c | sort -rn` 看既有分布，
别为同一概念开第二个名字（曾出现 `源` 与 `歌词源` 并存、`packaging` 与 `打包` 并存、`daemon` 与 `守护进程` 并存）。

**写法**

- 陈述"做了什么、新行为是什么"，不写比喻，不写口语语气词（了 / 吧 / 呢 / 其实 / 顺手 / 终于 / 搞定）。
- 修复类写**现象消失后的行为**，不写"修了个 bug"：
  - 好：`fix(部件): 播放器消失时不再解引用已销毁的实例`
  - 差：`fix: 修复崩溃`
- 不用「」引号强调、不用感叹号、不写根因（根因属于正文或代码注释）。
- 长度目标 ≤ 40 汉字，不折行。

**动词首选**：新增、实现、修复、修正、调整、改为、改为由…、记录、补充、抽取、移除、合并、发布。
**避免**：优化（含糊）、完善、加强、处理、搞、弄、"支持一下"。含糊的"优化"必须换成具体动作（改用 / 减少 / 预计算 / 缓存 / 合并请求）。

---

## 4. 正文

**写**：

- 新行为的边界与默认值：`默认关闭。`、`默认 280 px。`
- 关键参数、常量、标识符（反引号）：`全局偏移在存储层截断到 ±10000 毫秒，读写两侧均生效。`
- 用户可见的影响面与量化结果：`一首四分钟的歌唤醒次数从约 7200 次降到约 120 次。`
- 适用范围与已知不适用情形。

**不写**：

- 根因叙事与时间线（"此前因为…所以…现已修复"）：
  - 反例：`网易云以「英文名（中文名）」写法收录的艺人…其歌曲此前可能因艺人相似度计算过低而搜不到歌词，现已修复。`
  - 改法：`cleanArtists 把以括号收尾的艺人名拆成两条，括号内外各自参与艺人相似度计算。`
- 心路历程、评价、感慨、TODO、与本次改动无关的背景。
- 复述主题的正文（GitHub 网页编辑常产生：`Updated README to enhance formatting and improve feature descriptions.` → 删掉，或写具体改了什么）。

---

## 5. 用词

### 5.1 禁用（比喻 / 口语）→ 替换

| 禁用 | 替换 | 说明 |
|---|---|---|
| 闸门、被取消资格 | 校验、未通过校验 | 判定条件，直接说"校验" |
| 兜底、兜底分支 | 回退、default 分支 | 回退用于功能路径；switch 的 default 分支写 `default` |
| 佐证、旁证 | 校验 / 依据 | "靠…旁证匹配" → "依据…匹配"；"需…佐证" → "要求…" |
| 胜出 | 得分 | |
| 命中、精确命中 | 匹配到、完全匹配 | |
| 误连 | 匹配到（错误的） | |
| 暴露（问题） | 发现 | |
| 交还、交接（底板） | 绘制方切换 / 改回由…绘制 | |
| 无谓触发 | 触发 | 直接删「无谓」 |
| 拦不住 | 无法拦截 | |
| 判据 | 判定 | |
| 冒充 | 归类为 | |
| 触碰（成员） | 访问 | |
| 口径 | 统计方式 / 取值 | "单次查找口径" → "按单次查找统计" |
| 打不开 | 无法打开 | |
| 不出词 | 不显示歌词 | |
| 同刻 | 同时 | |
| 平手 | 同分 | |
| 报送（标题） | 标题为… | |
| 改装到 | 安装到 | |
| 规避改名 | 替换文件名中的… | |
| 靠…（匹配/胜出） | 依据… / 通过… | |
| 骨架 | 结构 | |
| 搞 / 弄 / 搞定 / 顺手 / 终于 | 删，改写为动作 | |

### 5.2 保留（已是本仓库领域术语，不要替换）

阈值、候选、候选池、去重、打分、指纹、锚点、快照、原子快照、回退、钩子、契约、硬编码、
负缓存、幂等、剥离变体（对应代码 `gloss`/`strip`）、本地化回退、时长窗口、伴生键、default 分支。

### 5.3 代码标识符照抄

`AdjustOffset`、`positionUs`、`anchorMonotonicNs`、`emptyTextUseDefault`、`passesGlossVariantGate`、`ENABLE_PROVIDER_QQ` 等原样写，
不要意译成"位置变量""锚点时间戳"这类二次命名。改动涉及代码标识符时，消息用词应与它同源
（代码叫 `…Gate`，消息说"校验"；代码叫 `default:`，消息不要说"兜底"）。

---

## 6. 标点、数字与单位

- 中文文本用全角标点；引号用「」；分句用「；」，列举用「、」。
- 中英文 / 数字之间加一个半角空格：`读取 project(VERSION) 的编译期定义`。
- 单位与数值：`250 毫秒`、`2 秒`、`280 px`、`30 Hz`；涉及阈值要写明方向（`相差不超过 2 秒`）。
- 代码元素、设置项、路径、命令、字段名一律反引号：`logging/debug`、`QT_LOGGING_RULES`、`players/blacklist`。

---

## 7. 正反例对照（真实改写）

| 旧 | 新 |
|---|---|
| `fix(部件): ksvg 底板平时交还 shell，仅淡出时自绘` | `fix(部件): ksvg 底板默认由 shell 绘制，仅淡出期间自绘` |
| `fix(部件): 淡出结束后将底板交还 shell` | `fix(部件): 淡出结束后底板改回 shell 绘制` |
| `docs: 决策 40 记录底板交接机制` | `docs: 决策 40 记录底板绘制方的切换条件` |
| `fix(核心): 部分艺人的歌能匹配到歌词了` | `fix(核心): 艺人名按「主名（别名）」收录时也能匹配到歌词` |
| `fix(守护进程): 日志文件打不开时报错，修正日志文件生命周期` | `fix(守护进程): 日志文件无法打开时记录路径与原因并延长其生命周期` |
| `fix(守护进程): 修正失败状态与日志生命周期` | `fix(守护进程): 区分网络失败与 HTTP、解析错误并退出前卸载日志处理器` |
| `chore(配置): 调整默认歌词显示设置` | `chore(配置): 调整默认字重、面板动画与译文显示` |
| `fix(设置): 保持歌词源开关绑定` | `fix(设置): 停用来源被拒后勾选框仍与来源列表同步` |
| `feat(设置): 新增来源平台勾选，修复 Apple Music 网页版不出词` | `feat(设置): 新增来源平台勾选并修复 Apple Music 网页版不显示歌词` |
| `fix(匹配): …且跳过被取消资格的首位候选` | `fix(匹配): …并跳过未通过校验的首位候选` |
| `fix(匹配): …并在平手时优先非剥离变体` | `fix(匹配): …并在同分时优先未剥离变体` |
| `fix(打包): 导入工具改装到 /usr/bin 并可直接调用` | `fix(打包): 导入工具安装到 /usr/bin 并可直接调用` |
| `feat(源): 新增 QQ音乐歌词源…` | `feat(歌词源): 新增 QQ音乐歌词源…` |
| `feat(部件): 逐字歌词渲染「抬升发光」` | `feat(部件): 逐字歌词按词高亮并抬升当前词` |
| 正文 `PR #1 实机首装暴露的问题：…底板交接。` | 正文 `合并 PR #1 首次实机安装发现的问题修复：…底板绘制方切换。` |
| 正文 `…进程级隔离拦不住。` | 正文 `…进程级隔离无法拦截。` |
| 正文 `…靠艺人与时长旁证匹配到歌词；本地化译名不会再误连到错艺人的翻唱版本。` | 正文 `…依据艺人与时长匹配到歌词；本地化译名不再匹配到其他艺人的翻唱版本。` |
| 正文 `靠剥离变体胜出的候选需要两侧时长相差 2 秒内佐证，否则拒绝。` | 正文 `以剥离变体得分的候选要求两侧时长相差不超过 2 秒，否则拒绝。` |
| 正文 `只靠剥离匹配上的候选需要两侧时长相差 2 秒以内佐证。` | 正文 `仅通过剥离匹配上的候选要求两侧时长相差不超过 2 秒。` |
| 正文 `性能数字改为单次查找口径；…两个 switch 的兜底分支一致才是实际保障。` | 正文 `性能数字改为按单次查找统计；…两个 switch 的 default 分支一致才是实际保障。` |

---

## 8. 可直接套用的结构

```
feat(设置): 新增网络代理选项，支持直连、系统代理与自定义地址

自定义地址接受 `socks5://` 与 `http://`，作用于网易云与 AMLL；地址非法时设置页无法保存。
```
```
perf(部件): 按行边界唤醒取代 30 Hz 轮询

行切换时刻可精确计算，定时器改为单次触发；一首四分钟的歌唤醒次数从约 7200 次降到约 120 次。
```
```
fix(部件): 跑马灯失效时重置偏移

偏移改为 LyricLine 的属性绑定，失效即归零。
```
```
merge: 合并 PR #1（自动隐藏）

合并 PR #1 首次实机安装发现的问题修复：翻译失配、底板缺少模糊、背景提示失效、底板绘制方切换。
```
```
chore(构建): 发布 0.3.2

版本号与 metadata.json 同步为 0.3.2，未发布段落归入 v0.3.2。
```

---

## 9. 提交前自检

1. 主题是不是"做了什么"？只看主题能否知道装了什么？
2. 主题里有比喻、口语、语气词吗？（对照 §5.1 表）
3. type 正确吗？scope 与仓库既有用词一致吗？
4. 正文是否只写新行为？有没有漏出根因叙事、评价或复述主题？
5. 术语与代码标识符、`docs/DESIGN.md` 用词一致吗？
6. 有没有多余的 trailer、多余的空行、缺/多句号？

---

## 10. 全库自查

```bash
# 1) 列出含黑名单词的消息行（主题 + 正文）
git log --format='%h %s%n%b' | grep -nE '闸门|兜底|佐证|旁证|胜出|命中|交还|交接|暴露|无谓|拦不住|判据|打不开|不出词|同刻|平手|骨架|口径|搞定|顺手|终于|靠[^近]'

# 2) scope 用词分布（查英文 scope 混入 / 同概念多名）
git log --format='%s' | sed -n 's/^[a-z]*[(]\([^)]*\)[)].*/\1/p' | sort | uniq -c | sort -rn

# 3) trailer 残留（含未推送提交）
git log origin/main..HEAD --format='%h %s' ; git log --format='%B' | grep -ci 'Co-Authored-By'

# 4) 正文缺失（发布类提交应有正文）
git log --format='%h %s' --grep='发布' | while read h s; do git log -1 --format=%b $h | grep -q . || echo "no body: $h $s"; done
```

发现违规时按「分类（trailer／比喻／非约定格式／一致性）→ 逐条 `旧 → 新` 草稿 → 确认 → 执行」处理。
未推送的提交直接 rebase/amend 即可，不需要改写已发布历史；
`docs/DESIGN.md`、`CHANGELOG.md` 的用词同步另算一轮，避免消息与文档长期不一致。

---

## 11. 约束生效点

本仓库 `CLAUDE.md` 已写有"不加 `Co-Authored-By`、正文不写比喻"，但仍有提交违反（多来自
网页编辑与未读该约定的 agent 会话）。要让规范真正生效，需要：

1. 把 §3–§5 要点（type/scope 白名单、禁用词表、正文禁令）并入 `CLAUDE.md` 或 `AGENTS.md`；
2. 让生成提交的工具在提交前跑 §10 的第 1、3 条自查；
3. 代码评审时把 scope 一致性与 trailer 当作阻塞项。
